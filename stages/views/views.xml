<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="stages.list">
      <field name="name">stages list</field>
      <field name="model">stages.stages</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="stages.action_window">
      <field name="name">stages window</field>
      <field name="res_model">stages.stages</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="stages.action_server">
      <field name="name">stages server</field>
      <field name="model_id" ref="model_stages_stages"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="stages" id="stages.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="stages.menu_1" parent="stages.menu_root"/>
    <menuitem name="Menu 2" id="stages.menu_2" parent="stages.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="stages.menu_1_list" parent="stages.menu_1"
              action="stages.action_window"/>
    <menuitem name="Server to list" id="stages" parent="stages.menu_2"
              action="stages.action_server"/>
    -->
  </data>
</odoo>